/**
 * @kairos/event-types — shared TypeScript types for WebSocket events.
 *
 * Phase 0: skeleton only. Event payload types land as each event is implemented.
 * The authoritative source for event semantics is docs/specs/api-websocket.md.
 */

export {};
