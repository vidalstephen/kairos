{
  "name": "kairos",
  "version": "0.0.0",
  "private": true,
  "description": "A named, persistent personal AI that treats trust as an earned asset.",
  "license": "MIT",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^6.0.3"
  }
}
